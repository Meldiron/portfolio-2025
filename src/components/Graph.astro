---
import githubData from "../data/github.json";

interface Props {
  year?: number;
}

type ContributionDay = {
  color: string;
  contributionCount: number;
  date: string;
  weekday: number;
};
type Week = {
  contributionDays: ContributionDay[];
  firstDay: string;
};
type Weeks = Week[];

const currentYear = new Date().getFullYear();

const weeks: Weeks[] = [];

// Graph.astro shows last 5 years — filter weeks accordingly
const fiveYearsAgo = `${currentYear - 4}-01-01`;
for (const week of githubData.weeks as Week[]) {
  if (week.firstDay >= fiveYearsAgo) {
    weeks.push(week as any);
  }
}

const colorData: any = {};
const today = new Date().toISOString().split("T")[0];

let week = 0;
let day = 0;
let totalWeeks = 0;
weeks.forEach(({ contributionDays }: any) => {
  contributionDays.forEach(({ contributionCount, date }: any) => {
    if (date > today) return;

    let contributionsColor = "contrib-0";

    if (contributionCount >= 15) {
      contributionsColor = "contrib-4";
    } else if (contributionCount >= 10) {
      contributionsColor = "contrib-3";
    } else if (contributionCount >= 5) {
      contributionsColor = "contrib-2";
    } else if (contributionCount >= 1) {
      contributionsColor = "contrib-1";
    }

    let daySlot = day + 1;
    if (day === 6) {
      daySlot = 0;
    }

    colorData[`week-${week}-day-${daySlot}`] = contributionsColor;

    day++;
    if (day === 7) {
      day = 0;
      week++;
      totalWeeks = week;
    }
  });
});
// Account for a partial final week
if (day > 0) {
  totalWeeks = week + 1;
}
---

<section>
  {
    weeks.length > 0 && (
      <div id="contributions" class="flex gap-1">
        {Array.apply(null, Array(totalWeeks)).map((_week, i) => {
          return (
            <div class={`flex-col gap-1 flex`}>
              {Array.apply(null, Array(7)).map((_day, j) => {
                const cls = colorData[`week-${i}-day-${j}`];
                return cls ? (
                  <div
                    class={`h-2 w-2 lg:h-3 lg:w-3 rounded-[1px] sm:rounded-sm ${cls}`}
                  />
                ) : (
                  <div class="h-2 w-2 lg:h-3 lg:w-3" />
                );
              })}
            </div>
          );
        })}
      </div>
    )
  }
</section>
